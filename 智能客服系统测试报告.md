# 智能客服系统测试报告

> 测试时间：2026-08-14
> 测试环境：DDEV + WSL Ubuntu-24.04 + WordPress (https://ddev-test.ddev.site/)
> 插件版本：wp-ai-customer-service

---

## 测试账号

| 字段 | testcustomer | buyer007（推荐） |
|---|---|---|
| 用户名 | testcustomer | buyer007 |
| 密码 | Test@2024 | Buyer@2024 |
| 邮箱 | testcustomer@example.com | — |
| 角色 | customer | customer |
| 订单数 | 1 笔 | 3 笔 |
| 订单详情 | #2998：processing，¥63.60 | #2999：completed，¥1590<br>#3000：processing，¥1590<br>#3001：pending，¥6360 |
| 适合测试 | 基础智能客服场景 | 多订单、多状态分支 |

**登录入口：** `https://ddev-test.ddev.site/my-account/`

---

## 调试 Checklist

### 登录验证

- [x] 打开登录入口： `https://ddev-test.ddev.site/my-account/`
- [x] 使用 testcustomer / Test@2024 登录成功
- [x] 使用 buyer007 / Buyer@2024 登录成功（显示名"李采购"）

### testcustomer 场景（单订单）

- [x] 输入 `Where is my order?` → 触发 RAG，返回 #2998 的 processing 状态 ✅
- [x] 输入 `我的订单到哪了` → 同上，返回 #2998 状态 ✅
- [x] 输入 `有哪些球衣推荐` → 走 deepseek API 返回商品推荐 ✅
- [x] 输入 `Do you ship internationally?` → 走 AI 回复，**未**误判为查单 ✅
- [x] 输入 `找人工客服` → 触发人工转接话术 ✅

### buyer007 场景（多订单 / 多状态）

- [x] 输入 `我之前的订单收到了吗` → 返回 #2999 的 completed 状态 ✅
- [x] 输入 `我的订单发货了吗` → 返回订单列表含 #3000 的 processing 状态 ⚠️（未聚焦 processing，返回全部订单）
- [x] 输入 `我有未付款的订单吗` → 返回订单列表含 #3001 的 pending 状态 ❌（未聚焦 pending，返回全部订单）
- [x] 输入 `我最近的所有订单` → 列出 #2999、#3000、#3001 三笔订单 ✅

### 对比测试

- [x] 两个账号分别登录，同一问题对比返回结果 ✅
  - testcustomer 查单 → 返回 #2998（单订单）
  - buyer007 查单 → 返回 #2999/#3000/#3001（三笔订单）
  - 结论：订单查询按用户隔离，数据正确

---

## 结果记录

| 用例 | 实际结果 | 通过？ |
|---|---|---|
| testcustomer 查单 | 输入"Where is my order?"和"我的订单到哪了"均触发 RAG，返回 #2998（processing，¥63.60） | ✅ 通过 |
| testcustomer 商品咨询 | 输入"有哪些球衣推荐"走 deepseek API，返回商品推荐内容 | ✅ 通过 |
| testcustomer 物流政策 | 输入"Do you ship internationally?"走 AI 回复，未误判为查单（正则收紧生效） | ✅ 通过 |
| testcustomer 人工转接 | 输入"找人工客服"触发人工转接话术，日志已记录 | ✅ 通过 |
| buyer007 已完成订单 | 输入"我之前的订单收到了吗"返回 #2999（completed，¥1590） | ✅ 通过 |
| buyer007 处理中订单 | 输入"我的订单发货了吗"返回全部 3 笔订单（含 #3000 processing），未按"发货"意图聚焦 | ⚠️ 部分通过 |
| buyer007 待付款订单 | 输入"我有未付款的订单吗"返回全部 3 笔订单（含 #3001 pending），未按"未付款"意图聚焦 | ❌ 未通过 |
| buyer007 多订单查询 | 输入"我最近的所有订单"列出 #2999、#3000、#3001 三笔订单及状态 | ✅ 通过 |
| 双账号对比 | testcustomer 返回 #2998，buyer007 返回 #2999/#3000/#3001，订单按用户隔离正确 | ✅ 通过 |

---

## 发现的问题

### 问题 1：订单状态语义过滤未生效（中等优先级）

**现象**：
- "我之前的订单收到了吗" → 正确聚焦 #2999（completed）✅
- "我的订单发货了吗" → 返回全部订单，未聚焦 #3000（processing）❌
- "我有未付款的订单吗" → 返回全部订单，未聚焦 #3001（pending）❌

**根因分析**：
订单查询逻辑（`class-woo-integration.php`）识别到查单意图后，统一返回用户最近订单列表，未根据问句中的状态关键词（"发货"→processing、"未付款"→pending、"收到"→completed）做二次过滤。

**建议修复**：
在订单查询返回结果前，增加状态关键词匹配逻辑：
- "收到/已完成/完成" → 过滤 `completed` 状态
- "发货/处理中/运输中" → 过滤 `processing` 状态
- "未付款/待付款/pending" → 过滤 `pending` 状态
- 无明确状态关键词 → 返回全部最近订单

### 问题 2：货币单位显示为 USD（低优先级）

**现象**：AI 回复中金额显示为"1590.00 USD"，测试手册预期"¥1590"。

**说明**：此为 WooCommerce 货币设置问题，非插件 bug。如需显示人民币，需在 WooCommerce 设置中将货币改为 CNY。

---

## 已验证通过的功能

1. ✅ RAG 订单查询（中英文意图识别）
2. ✅ 订单按用户隔离（数据安全）
3. ✅ 商品咨询走 deepseek API
4. ✅ 物流政策咨询走 AI 回复（未误判查单，正则收紧生效）
5. ✅ 人工转接话术及日志记录
6. ✅ 多订单列表展示

## 待优化功能

1. ⚠️ 订单状态语义过滤（"发货"/"未付款"等关键词应聚焦对应状态订单）
2. 💡 货币单位显示（可选，视业务需求）
